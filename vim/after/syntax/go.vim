set tabstop=4 "el tab sera de 2 espacios
set shiftwidth=4 "identa con 2 espacios
set softtabstop=4 "entiende que el tab que usaste, como espacios, es un tab y te permite borrarlo con un solo keystroke

